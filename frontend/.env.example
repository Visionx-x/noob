# GrowthForge Mobile – copy to .env.local
# Used at build time so the app talks to the correct API.

# Development (phone on same Wi‑Fi as your PC): use your PC's IPv4, e.g. http://192.168.1.10:2000/api
# Production (app for release): use your VPS API URL, e.g. https://api.yourdomain.com/api
NEXT_PUBLIC_API_URL=http://77.90.6.237:2000/api
