# FastAPI and ASGI server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Production server
gunicorn>=21.0.0

# Database
sqlalchemy
alembic

# Environment configuration
python-dotenv
pydantic-settings

# Security and Authentication
passlib[bcrypt]
python-jose[cryptography]
python-multipart

# Rate limiting
slowapi

# Optional: PostgreSQL (uncomment if using PostgreSQL in production)
# psycopg2-binary

# Optional: Redis (uncomment if using Redis for caching)
# redis